# 2026-09-19 工作日志

## R01-01 高端装备 / 机器人 — ThreeC Historical Universe Expansion（Research Intake Package 交付完成）

### 做了什么
在独立工作区 `D:\AAIWORK\Freedom\R01-01-high-end-equipment` 完成 R01-01 任务，产出符合 `01_PROTOCOL/historical_research_intake.schema.json` 的 11 文件研究包至 `05_OUTPUT/`。

### 交付物
- `manifest.json`：任务范围、priority A(2018-2025)/B(2015-2017)、5 项 macro_theme_proposals（MT001-005）、6 项 cross_task_notes（CT001-006）
- `candidates.json`：7 个候选（R01-HIEQ-001..007）
- `evidence.json`：41 条证据（E001-E041）
- `sources.json`：35 个来源（S001-S035）
- `securities.json`：17 条标的（SEC001-SEC017）
- `exclusions.json`：10 项排除（X001-X010）
- `conflicts.json`：8 项冲突（CF001-CF008）
- `research_questions.json`：12 项研究问题（Q001-Q012）
- `quality_summary.json`、`coverage.md`、`checksums.sha256`

### 7 个候选（按机制轴组织，非按股票记忆）
| ID | 对象 | 年份 | 状态 | 置信度 |
|---|---|---|---|---|
| 001 | 工程机械景气周期（更新+基建+环保淘汰） | 2021 | PROVISIONAL | high |
| 002 | 工业机器人产量下行周期 | 2019 | PROVISIONAL | high |
| 003 | 工业自动化复苏周期（疫情后资本开支回补+渗透率） | 2020 | PROVISIONAL | high |
| 004 | 「机器人+」政策驱动（2023-01 十七部门） | 2023 | PROVISIONAL | medium |
| 005 | 人形机器人产业叙事形成（2023-05 起） | 2023 | PROVISIONAL | medium |
| 006 | 大规模设备更新政策（国发〔2024〕7号） | 2024 | INSUFFICIENT | low |
| 007 | 人形机器人量产预期+国产加速+订单 | 2025 | INSUFFICIENT | low |

### 核心判断（均未自裁）
- 002 与 003 可能属同一 ThemeCycle 两阶段（CT006）
- 005 与 007 边界未切分，人形机器人多轮是否同一 Campaign 交 ThreeC Agent 走 Gate Q1-Q5（CT005）
- 004 与 005 的 2023 年 3-4 月边界无法切分（Q007）
- 「高端装备」是否独立 Macro Theme 交 CMTR v1（Q005）
- 「机器人」与「工业自动化」Q1 Anti-example 风险（CF007）
- 轨交装备 INSUFFICIENT（X004）；半导体设备→R01-02（X003）；纯军工→R01-06（X006）；汽车/Robotaxi→已有 family（X007）

### 验证结果（必须记录）
```
python tools/validate_historical_research_intake.py 05_OUTPUT
→ checks: 24  FAIL: 0  WARN: 0  RESULT: PASS

python tools/validate_historical_research_intake.py --check
→ checks: 24  FAIL: 0  WARN: 0  RESULT: PASS

python tools/test_validate_historical_research_intake.py
→ tests: 37  failed: 0
```

### 环境经验（重要，跨会话复用）
- **bash 工具在本机不可用**：`dirname: command not found` / `cd: null directory`（shim 损坏）。必须改用 **PowerShell 工具** 执行命令。
- **PowerShell stdout 不内联回显**：命令只返回 "Command completed with exit code 0"。解法：输出重定向到 `$env:TEMP\*.txt`，再用 Read 工具读取。
- Python 路径：`C:\Users\86115\.workbuddy\binaries\python\versions\3.13.12\python.exe`
- Read/Grep/Glob 工具正常可用。

### 校验器关键约束（从源码逆向得出，避免返工）
- `INTAKE_ID_RE = ^R\d{2}-[A-Z0-9]+-[A-Z]*\d{3,}$`；`R01-HIEQ-001` 合法
- `FORBIDDEN_KEYS` 含 `campaign_id`/`start_date`/`end_date`/`peak_date`/`strength`/`result`/`classification`/`macro_theme_id` —— **不得作为字段名**（candidate 的 `classification_proposal` 合法，`classification` 非法）
- `macro_theme_proposal`（candidate 内）是**字符串**，不是对象引用
- `confidence_distribution`（C21）**只统计 candidate 级 confidence**，不含 evidence
- `counts` 各值必须等于对应数组实际长度
- 所有文件必须 **LF-only**（不得 CRLF），`checksums.sha256` 行格式 `sha256  filename`（两个空格）
- `quality_summary.no_quantity_kpi_acknowledged` 必须为 `true`
- `--check` 要求 `workspace_manifest.json` 的 `task_status == "PREPARED_NOT_STARTED"` → **不要改这个字段**
- `exclusions` minItems=1（强制非空，防只收集成功案例）

### 未完成的（明确记录）
- Priority B（2015-2017）覆盖不完整，仅工程机械 2016-08 起段；2015 年完全未覆盖
- 市场侧行情序列严重不足：006 与 007 市场侧为零；005 仅单日
- 未取得《人形机器人创新发展指导意见》一手原文（印发日期两口径 CF001）
- 轨交装备 2019-2020、2024-2025 段未采集

### 边界遵守
未使用任何 canonical ID，未改 SQLite/DB/schema/既有 Campaign/exports/SA Rule Set，未创建排名/评分/概率/预测/胜率/目标价，未声明 VERIFIED，未运行 Time Observation 与 Structural Analogy。交付后停止，未进入 ThreeC 主仓库。
