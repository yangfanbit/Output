# tools/fixtures/ — 合成夹具

> ## ⚠️ 本目录全部是**合成夹具**（SYNTHETIC FIXTURE）
>
> **不是研究数据。不是 Campaign。不是 Evidence。不是 Candidate。**
> 仅用于验证 `tools/validate_historical_research_intake.py` 的正确性。

`minimal_valid_package/` 是一个**最小合法 Intake Package**，
可作为编写 `05_OUTPUT/` 时的**结构模板**参考。

> **不得**把 fixture 的内容当作研究结果提交。
> 复制结构可以，**复制内容绝对不可以**。
